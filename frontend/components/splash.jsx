var React = require('react');

var Splash = React.createClass({
  render: function(){
    return(<div className="splash">Welcome to BookShare! A community to share
  books with other users.</div>);}

});

module.exports = Splash;
